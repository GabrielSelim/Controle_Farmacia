# 🏗️ Arquitetura do Sistema - Controle de Medicamentos

## 📊 Visão Geral

```
┌─────────────────────────────────────────────────────────────────┐
│                         USUÁRIO                                  │
│                    (Navegador Web)                               │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTPS
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FRONTEND (React)                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Login      │  │  Dashboard   │  │   Records    │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Shifts     │  │  AdminUsers  │  │  Components  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  📦 Tecnologias: React, Vite, Tailwind CSS, React Router        │
│  🌐 Deploy: Vercel / Netlify                                    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ REST API (JSON)
                         │ Authorization: Bearer <JWT>
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (Node.js + Express)                   │
│                                                                  │
│  ┌────────────────── ROTAS ─────────────────────┐               │
│  │                                               │               │
│  │  /api/auth      → Login, Register, Me        │               │
│  │  /api/users     → CRUD Usuários              │               │
│  │  /api/meds      → CRUD Medicamentos          │               │
│  │  /api/shifts    → CRUD Plantões              │               │
│  │  /api/records   → CRUD Registros + Receive   │               │
│  │  /api/notify    → WhatsApp + Calendar        │               │
│  │                                               │               │
│  └───────────────────┬───────────────────────────┘               │
│                      │                                           │
│  ┌────────────── MIDDLEWARES ───────────────┐                   │
│  │                                           │                   │
│  │  🔐 authenticate  → Valida JWT            │                   │
│  │  👮 roles         → Verifica permissões   │                   │
│  │  🚦 rate-limit    → Limita requisições    │                   │
│  │                                           │                   │
│  └───────────────────┬───────────────────────┘                   │
│                      │                                           │
│  ┌────────────── CONTROLLERS ───────────────┐                   │
│  │                                           │                   │
│  │  authController    → Login, JWT          │                   │
│  │  usersController   → Gestão usuários     │                   │
│  │  recordsController → Registros + Audit   │                   │
│  │  shiftsController  → Plantões            │                   │
│  │  notifyController  → Notificações        │                   │
│  │                                           │                   │
│  └───────────────────┬───────────────────────┘                   │
│                      │                                           │
│  ┌────────────── SERVICES ────────────────┐                     │
│  │                                         │                     │
│  │  📱 callmebot.js → WhatsApp API        │                     │
│  │  📅 calendar.js  → Google Calendar     │                     │
│  │  🔐 crypto.js    → AES-256-GCM         │                     │
│  │                                         │                     │
│  └───────────────────┬─────────────────────┘                     │
│                      │                                           │
│                      ▼                                           │
│  ┌──────────── PRISMA ORM ────────────────┐                     │
│  │                                         │                     │
│  │  Models: User, Medicamento, Shift,     │                     │
│  │          Record, AuditLog               │                     │
│  │                                         │                     │
│  └───────────────────┬─────────────────────┘                     │
│                                                                  │
│  📦 Tecnologias: Express, Prisma, JWT, bcrypt                    │
│  🌐 Deploy: Railway / Render / Heroku                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ SQL Queries
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     BANCO DE DADOS                               │
│                                                                  │
│  📊 SQLite (desenvolvimento)                                     │
│  📊 PostgreSQL (produção)                                        │
│                                                                  │
│  ┌─────────────────────────────────────────────────┐            │
│  │  Tabelas:                                       │            │
│  │                                                 │            │
│  │  • User          - Usuários e credenciais      │            │
│  │  • Medicamento   - Medicamentos controlados    │            │
│  │  • Shift         - Escalas de plantão          │            │
│  │  • Record        - Registros de contagem       │            │
│  │  • AuditLog      - Logs de auditoria           │            │
│  │                                                 │            │
│  └─────────────────────────────────────────────────┘            │
│                                                                  │
│  🌐 Deploy: Railway Postgres / Heroku Postgres / Supabase        │
└─────────────────────────────────────────────────────────────────┘
                         │
                         │ External APIs
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   SERVIÇOS EXTERNOS                              │
│                                                                  │
│  📱 CallMeBot API - Envio de WhatsApp                            │
│     https://api.callmebot.com/whatsapp.php                       │
│                                                                  │
│  📅 Google Calendar API - Eventos de calendário (opcional)       │
│     https://www.googleapis.com/calendar/v3                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Fluxo de Dados - Registro de Contagem

```
┌──────────────────────────────────────────────────────────────────┐
│                      1. ENTREGA                                  │
└──────────────────────────────────────────────────────────────────┘

Farmacêutico A (saindo)
     │
     │ 1. Login → JWT Token
     ▼
┌─────────────┐
│   Browser   │ POST /api/auth/login
└──────┬──────┘ → Token armazenado no localStorage
       │
       │ 2. Criar registro de entrega
       ▼
┌─────────────┐
│   Browser   │ POST /api/records
└──────┬──────┘ { medId, qtyDelivered, shiftStart, shiftEnd }
       │
       ▼
┌─────────────────────────┐
│   Backend (Express)     │
│                         │
│  • Valida JWT ✓         │
│  • Cria Record          │
│  • deliveredBy = userId │
│  • deliveredAt = now()  │
│  • status = 'pendente'  │
│                         │
└────────┬────────────────┘
         │
         │ Prisma ORM
         ▼
┌─────────────────────────┐
│   Database              │
│                         │
│  INSERT INTO Record     │
│  INSERT INTO AuditLog   │
│    (action='CREATE')    │
│                         │
└─────────────────────────┘


┌──────────────────────────────────────────────────────────────────┐
│                    2. RECEBIMENTO                                │
└──────────────────────────────────────────────────────────────────┘

Farmacêutico B (entrando)
     │
     │ 1. Login → JWT Token
     ▼
┌─────────────┐
│   Browser   │ GET /api/records?status=pendente
└──────┬──────┘ → Lista registros pendentes
       │
       │ 2. Seleciona registro para receber
       ▼
┌─────────────┐
│   Browser   │ POST /api/records/:id/receive
└──────┬──────┘ { qtyReceived: 10 }
       │
       ▼
┌─────────────────────────┐
│   Backend (Express)     │
│                         │
│  • Valida JWT ✓         │
│  • Atualiza Record      │
│  • receivedBy = userId  │
│  • receivedAt = now()   │
│  • status = ?           │
│    - 'finalizado'       │
│      (qty igual)        │
│    - 'discrepancia'     │
│      (qty diferente)    │
│                         │
└────────┬────────────────┘
         │
         │ Prisma ORM
         ▼
┌─────────────────────────┐
│   Database              │
│                         │
│  UPDATE Record          │
│  INSERT INTO AuditLog   │
│    (action='RECEIVE')   │
│                         │
└─────────────────────────┘
```

---

## 🔐 Fluxo de Autenticação

```
┌──────────┐
│  Client  │
└────┬─────┘
     │ 1. POST /api/auth/login
     │    { email, password }
     ▼
┌─────────────────────────────┐
│   authController.login      │
│                             │
│  1. Busca user no banco     │
│  2. bcrypt.compare()        │
│  3. jwt.sign()              │
│                             │
└────────┬────────────────────┘
         │
         │ { token, user }
         ▼
┌──────────┐
│  Client  │  Armazena token no localStorage
└────┬─────┘
     │
     │ 2. Requisições subsequentes
     │    Authorization: Bearer <token>
     ▼
┌─────────────────────────────┐
│   authenticate middleware   │
│                             │
│  1. Extrai token do header  │
│  2. jwt.verify()            │
│  3. req.user = decoded      │
│                             │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│   roles middleware          │
│   (se necessário)           │
│                             │
│  Verifica req.user.role     │
│                             │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│   Controller                │
│   Processa requisição       │
└─────────────────────────────┘
```

---

## 📱 Fluxo de Notificação WhatsApp

```
┌──────────┐
│  Admin/  │
│  Chefe   │
└────┬─────┘
     │ 1. POST /api/notify/shift/:shiftId
     ▼
┌─────────────────────────────────────┐
│   notifyController.notifyShift      │
│                                     │
│  1. Busca Shift no banco            │
│  2. Busca User (pharmacist)         │
│  3. Prepara mensagem                │
│                                     │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│   callmebot.sendWhatsAppByEmail     │
│                                     │
│  1. Busca user.telefone             │
│  2. Busca user.callmebot_key        │
│  3. decrypt(callmebot_key)          │
│                                     │
└────────┬────────────────────────────┘
         │
         │ HTTPS Request
         ▼
┌─────────────────────────────────────┐
│   CallMeBot API                     │
│   api.callmebot.com/whatsapp.php    │
│                                     │
│  ?phone=5516999999999               │
│  &text=Mensagem                     │
│  &apikey=DECRYPTED_KEY              │
│                                     │
└────────┬────────────────────────────┘
         │
         │ WhatsApp delivered
         ▼
┌──────────┐
│  WhatsApp│  Usuário recebe mensagem
│  User    │
└──────────┘
```

---

## 🛡️ Segurança em Camadas

```
┌─────────────────────────────────────┐
│  1. HTTPS (SSL/TLS)                 │  ← Comunicação criptografada
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│  2. CORS Policy                     │  ← Apenas origens permitidas
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│  3. Rate Limiting                   │  ← Previne abuso
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│  4. JWT Authentication              │  ← Verifica identidade
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│  5. Role-Based Authorization        │  ← Verifica permissões
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│  6. Input Validation                │  ← Sanitiza dados
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│  7. Password Hashing (bcrypt)       │  ← Senhas nunca em plain text
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│  8. Data Encryption (AES-256)       │  ← CallMeBot keys criptografadas
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│  9. Audit Logging                   │  ← Rastreamento de ações
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│  10. Prepared Statements (Prisma)   │  ← Previne SQL Injection
└─────────────────────────────────────┘
```

---

## 📦 Stack Tecnológica Detalhada

### Frontend
```
React 18.2+
├── Vite                 → Build tool rápido
├── React Router 6       → Navegação SPA
├── Tailwind CSS 3       → Estilização utility-first
├── Axios                → Cliente HTTP
└── Context API          → Gerenciamento de estado
```

### Backend
```
Node.js 18+
├── Express 4            → Framework web
├── Prisma 5             → ORM type-safe
├── JWT                  → Tokens de autenticação
├── bcrypt               → Hash de senhas
├── node-fetch           → Chamadas HTTP
├── express-rate-limit   → Rate limiting
├── multer               → Upload de arquivos (opcional)
└── dotenv               → Variáveis de ambiente
```

### Database
```
Desenvolvimento:  SQLite
Produção:        PostgreSQL
ORM:             Prisma
Migrations:      Prisma Migrate
```

### Infraestrutura
```
Frontend:  Vercel / Netlify
Backend:   Railway / Render / Heroku
Database:  Railway Postgres / Supabase
CDN:       Vercel Edge Network
```

---

## 📈 Escalabilidade

### Atual (6-10 usuários)
- SQLite ou PostgreSQL básico
- Single instance backend
- Vercel CDN para frontend

### Futuro (50+ usuários)
- PostgreSQL com connection pooling
- Backend horizontal scaling (multiple instances)
- Redis para cache e sessions
- S3 para upload de fotos
- CloudFlare para DDoS protection

---

**Documentação de Arquitetura Completa** ✅
