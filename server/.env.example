# ===========================================
# CONFIGURAÇÕES DO SERVIDOR
# ===========================================

# Porta onde o servidor vai rodar
PORT=3001

# Ambiente (development, production)
NODE_ENV=development

# ===========================================
# AUTENTICAÇÃO
# ===========================================

# Chave secreta para assinar tokens JWT
# IMPORTANTE: Use uma senha forte e única!
# Dica: gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=sua_senha_secreta_jwt_super_forte_aqui_min_32_caracteres

# ===========================================
# BANCO DE DADOS
# ===========================================

# SQLite (desenvolvimento local)
DATABASE_URL="file:./dev.db"

# PostgreSQL (produção - descomente e configure)
# DATABASE_URL="postgresql://usuario:senha@host:5432/nome_do_banco?schema=public"

# ===========================================
# CRIPTOGRAFIA
# ===========================================

# Chave mestra para criptografar dados sensíveis (callmebot_key)
# IMPORTANTE: Use uma chave diferente do JWT_SECRET!
# Dica: gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CALLMEBOT_MASTER_KEY=outra_chave_secreta_diferente_para_criptografia

# ===========================================
# INTEGRAÇÃO GOOGLE CALENDAR (OPCIONAL)
# ===========================================

# Credenciais OAuth do Google Cloud Console
# Tutorial: https://developers.google.com/calendar/api/quickstart/nodejs
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALENDAR_ID=primary

# ===========================================
# OBSERVAÇÕES IMPORTANTES
# ===========================================

# 1. NUNCA commite este arquivo com valores reais!
# 2. Em produção, use variáveis de ambiente do serviço (Railway, Heroku, etc)
# 3. Para gerar senhas seguras:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. Mantenha backup das chaves de produção em local seguro!
